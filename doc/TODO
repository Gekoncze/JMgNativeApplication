add more unit tests
    add unit test for component search
    add unit test for read service
    add unit test for update service

improve move actions
    add drag and drop move
    add keyboard shortcuts to move up and down

add field select display order customization for each entity class
add field select display customization for each entity class
add icons to multi selects
add separate create / remove buttons for multi select rows

improve transaction management
bug in delete service
    when deleting mg structure in test project, the whole location gets deleted