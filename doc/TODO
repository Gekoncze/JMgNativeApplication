add more unit tests
    component search
    read service
    update service
    transaction manager

improve multi selects
    add object icons (via object icon provider)
    add drag and drop move action
    add keyboard shortcuts to move up / down
    add keyboard shortcuts to select next / previous item

add field select display order customization for each entity class
add field select display customization for each entity class
add separate create / remove buttons for multi select rows
add menu items for right click to navigation (refresh before invoking action ?)
on main window refresh, close tabs of objects that were deleted