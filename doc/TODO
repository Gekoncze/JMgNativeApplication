add more unit tests
    add unit test for component search

add entity ui components for lists
    finish todos in entity link multi select
    finish todos in entity part multi select
    add entity value multi select
    add entity value multi select creation to entity view

improve move actions
    add drag and drop move
    add keyboard shortcuts to move up and down

add field select display order customization for each entity class
add icons to multi selects ?

change keyboard shortcuts bound to certain context to use KeyEventDispatcher
    it should automatically route key events to the current tab
        current tab should route the key event to certain field if it has focus